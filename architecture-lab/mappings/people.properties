# ontop JDBC connection for the R2RML recipe — points at the SQLite DB that
# `make people-r2rml` builds from people.csv (relative to architecture-lab/).
jdbc.url=jdbc:sqlite:build/people.db
jdbc.driver=org.sqlite.JDBC
jdbc.user=
jdbc.password=
