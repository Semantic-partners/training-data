# Local teaching container — everything anonymous. No auth to fight when
# students run `make load` or query. This is NOT for any exposed/production
# Fuseki; it's a throwaway lab service on the compose network.
[main]
ssl.enabled = false

[users]

[roles]

[urls]
/** = anon
